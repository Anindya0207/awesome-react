import React from 'react';
import MenuBar from './MenuBar';

const MenuApp: React.FC = () => {
  return (
    <div>
      <MenuBar />
      {/* Other components */}
    </div>
  );
};

export default MenuApp;
